/**
 * 
 */
package fr.openium.sga.Tests;

import org.junit.Test;

/**
 * @author Stassia
 * 
 */
public class DataManagerTest {

	@Test
	public void test() {
		/**
		 * replaced by randomValueTest
		 */

		/*
		 * Datamanager manager = Datamanager.getInstance();
		 * 
		 * String pathAll = new File("").getAbsolutePath() +
		 * ConfigApp.TESTDATA_ALL_RV; manager.setRVFile(new File(pathAll));
		 * assertTrue(new File(pathAll).exists());
		 *//**
		 * teste rv
		 */
		/*
		 * pathAll = new File("").getAbsolutePath() + "/TestData/newRv";
		 * 
		 * manager.setTestedRVFile(new File(pathAll)); assertTrue(new
		 * File(pathAll).exists());
		 *//**
		 * test picked value
		 */
		/*
		 * String value = manager.get_A_rtestv(); assertNotNull(value);
		 * assertTrue(value.equalsIgnoreCase("12"));
		 * assertTrue(manager.get_A_rtestv().equalsIgnoreCase("blabla"));
		 * 
		 * Float coverage = (float) (2 * 100 / 4); System.out.println("cov: " +
		 * coverage); System.out.println("cov: " + manager.coverage());
		 * 
		 * assertTrue(manager.coverage() == coverage);
		 *//**
		 * test stored value
		 */
		/*
		 * // assertTrue(manager.coverage() == (1 / 4));
		 */
	}
}
